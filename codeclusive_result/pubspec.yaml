name: codeclusive_result
description: A new Flutter package project.
version: 0.0.1
homepage: codeclusive.io
publish_to: none

environment:
  sdk: ">=3.1.5 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  codeclusive_lints:
    git:
      url: https://github.com/codeclusiveio/flutter-packages.git
      path: codeclusive_lints
      ref: main
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
